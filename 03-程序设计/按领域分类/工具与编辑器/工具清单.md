# 工具与编辑器清单

> 作者：代码分析生成 | 创建日期：2026-07-29 | 最后修改：2026-07-29

本文档汇总 Spaceship 项目中所有编辑器工具与自动化脚本。

---

## 工具总览

| 开发者 | 工具数 | 主要方向 |
|--------|--------|----------|
| Kaki | 9 个 | 配置编辑、测试构建、字体导入、验证 |
| wuqiwusheng | 约 22 个 | 序列图编辑器、视野系统设置、敌舰行为工具 |

---

## Kaki 编辑器工具

### 菜单工具（`Kaki/飞船/`）

| 菜单路径 | 实现类 | 功能 |
|----------|--------|------|
| `Kaki/飞船/Build Configured Test Assets And Scene` | `SpaceshipConfiguredTestAssetBuilder` | 根据当前配置批量生成测试资产和场景 |
| `Kaki/飞船/Validate Configured Test Assets` | — | 验证测试资产的完整性 |
| `Kaki/飞船/Run Assembly Playtest` | `AssemblyPlaytestSceneBuilder` | 自动构建并进入拼装测试场景 |
| `Kaki/飞船/Validate Complete Runtime` | — | 运行时全功能验证 |

### Inspector 编辑器

| 类型 | 用途 |
|------|------|
| `ChineseScriptableObjectEditor` | 中文 SO Inspector 基类，提供统一的中文界面样式 |
| `ModuleDefinitionEditor` | 模块定义的自定义 Inspector，可视化编辑模块参数 |

### 测试与验证

| 类型 | 用途 |
|------|------|
| `AssemblyPlaytestSceneBuilder` | 自动生成飞船组装测试场景 |
| `SpaceshipConfiguredTestAssetBuilder` | 根据配置构建测试资产 |
| `ShipTestLinkInteractionAutoInstaller` | 为测试场景自动安装链接交互组件 |
| `AllModuleFunctionValidationMenu` | 一键验证所有模块类型的功能完整性 |

### 字体工具

| 类型 | 用途 |
|------|------|
| `TmpChineseFontImporter` | 将中文字体导入 TextMeshPro 字体资产 |
| `EditorFontRecovery` | 编辑器字体异常恢复工具 |

### 迁移与维护

| 类型 | 用途 |
|------|------|
| `GameplayScriptLayoutMigrator` | 将旧布局的脚本迁移到新的目录结构 |

### 验证器（`Scripts/Validation/Editor/`）

| 类型 | 用途 |
|------|------|
| `CargoConfigurationValidator` | 验证货物配置的完整性（引用、数据范围） |
| `ManualSceneWiringValidator` | 验证场景手动连线的正确性（缺失引用检测） |

---

## wuqiwusheng 编辑器工具

### 序列图编辑器（SequenceMap Editor）

基于 **UI Toolkit + GraphView** 的可视化编辑器系统，菜单位于 `wuqiwusheng/` 路径。

#### 核心窗口

| 窗口 | 功能 |
|------|------|
| `SequenceMapWorkspaceHostWindow` | 主工作区宿主 |
| `SequenceMapWorkspaceWindow` | 多窗口布局管理 |
| `SequenceMapGraphWindow` | 图形编辑主窗口 |
| `SequenceMapGraphListWindow` | 图形文件列表 |
| `SequenceMapDetailsWindow` | 节点详情面板 |
| `SequenceMapVariablesWindow` | 变量编辑器 |
| `SequenceMapRecentGraphsWindow` | 最近打开记录 |
| `SequenceMapApiWindow` | API 浏览器 |
| `SequenceMapCanvasDebugWindow` | 画布调试 |

#### 高级功能

| 功能 | 实现类 |
|------|--------|
| 代码生成 | `SequenceMapCodeGenerator` |
| API 注册 | `SequenceMapApiRegistry` + `SequenceMapApiAttribute` |
| 双击打开图形 | `SequenceMapGraphAssetOpenHandler` |
| 场景拖拽到节点 | `SequenceMapGraphSceneDragHandler` |
| 面板大小调整 | `ResizablePanelManipulator` |
| 面板重排 | `ReorderablePanelContainer` |
| 中键平移 | `MiddleMouseContentDragger` |

### 视野系统工具

| 菜单路径 | 实现类 | 功能 |
|----------|--------|------|
| `wuqiwusheng/URP/创建 Polygon Region 预制体并放入场景` | `SpaceshipVisionSetupTool` | 创建视野多边形区域的 Prefab 并实例化 |

### 敌舰行为工具

| 类型 | 用途 |
|------|------|
| `EnemyBehaviorAttachTool` | 为选中对象批量添加敌舰行为树组件 |

---

## 工具设计原则

根据项目规范，编辑器工具需遵循以下原则：

1. **编辑器代码隔离**：所有 Editor 脚本必须位于 `Editor/` 子目录或 Editor 程序集中
2. **不影响运行时**：工具不得在运行时程序集中留下 Editor 依赖
3. **验证优于修复**：工具优先检测问题，修复需明确确认
4. **幂等性**：重复执行同一工具应产生相同结果或无误错误

---

## 相关文档

- [Kaki 系统总览](../../按归属管理/Kaki/系统总览.md)
- [wuqiwusheng 系统总览](../../按归属管理/wuqiwusheng/系统总览.md)
- [ScriptableObject 配置体系](../数据设计/ScriptableObject配置体系.md)
