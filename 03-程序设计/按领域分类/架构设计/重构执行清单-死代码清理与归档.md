# 重构执行清单：死代码清理与归档

> 作者：代码分析生成 | 创建日期：2026-08-02 | 最后修改：2026-08-02

本文档是「模块分层与粘合代码模式」重构的配套清理清单，列出无引用死代码、废弃宿主与已归档原型的处置方式。

---

## 一、无引用死代码（可直接删除）

以下文件经全工程检索，**无任何引用**，属于遗留死代码：

| 文件 | 内容 | 引用情况 | 处置 |
|------|------|----------|------|
| `Assets/wuqiwusheng/NonPlayerUnitBehavior/NonPlayerUnitBehaviorTree.cs` | 废弃行为树框架（`BehaviorTree` / `BehaviorNode` / `SelectorNode` / `SequenceNode`） | 无引用 | 删除 |
| `Assets/wuqiwusheng/NonPlayerUnitBehavior/NonPlayerUnitBehaviorNodes.cs` | 兼容占位（空） | 无引用 | 删除 |
| `Assets/wuqiwusheng/NonPlayerUnitBehavior/EnemyShipBehaviorNodes.cs` | 兼容占位（空） | 无引用 | 删除 |
| `Assets/wuqiwusheng/NonPlayerUnitBehavior/EnemyShipBehaviorRuntime.cs` | 兼容占位（空） | 无引用 | 删除 |

> 说明：以上文件标记「废弃遗留 / 兼容占位」，是行为框架从行为树演进为状态机时遗留的空壳。删除时需同步删除 `.meta` 文件（版本管理要求）。

## 二、废弃宿主与工具（随非玩家单位整合一并移除）

| 文件 | 内容 | 引用情况 | 处置 |
|------|------|----------|------|
| `Assets/wuqiwusheng/NonPlayerUnitBehavior/NonPlayerUnitBehaviorAuthoring.cs` | 非玩家单位行为并行宿主（含私有 `RuntimeAdapter`） | 仅被 `NonPlayerUnitBehaviorAttachTool` 引用 | 随 [非玩家单位行为整合方案](./../模块设计/非玩家单位行为整合方案.md) 移除，调试能力并入唯一宿主 |
| `Assets/wuqiwusheng/NonPlayerUnitBehavior/Editor/NonPlayerUnitBehaviorAttachTool.cs` | 菜单工具（添加 `NonPlayerUnitBehaviorAuthoring`） | 服务于被移除组件 | 一并移除 |

## 三、已归档原型

| 目录 | 状态 | 处置 |
|------|------|------|
| `Assets/Mailk/` | 已归档的独立物理验证原型（`Ship` / `ShipPhysics` / `ShipEditor` 等） | **已删除**（2026-08-02 模块化迁移） |

Mailk 原型的「编辑/飞行双模式」与飞船工坊存在概念重叠，因其已归档，于迁移时整体删除；若未来需复用其物理验证结论，应以独立评估立项，不直接并入主系统。

## 四、执行顺序

1. 非玩家单位行为整合完成（唯一宿主就绪）后，执行第二节的宿主与工具移除。
2. 第一节的四个死代码文件可与第二节同批删除，也可先行删除（无依赖）。
3. 删除后运行 `Spaceship > 飞船 > 验证手工场景装配` 与全部验收脚本，确认无回归。
4. 更新 [整体架构](../架构设计/整体架构.md) 的类型清单。

## 五、风险

- 死代码删除为纯移除，风险低。
- `NonPlayerUnitBehaviorAuthoring` 若在**未迁移场景**中被引用（场景序列化引用），删除前需用 `ManualSceneWiringValidator` 扫描场景中的残留组件。

---

## 相关文档

- [模块分层与粘合代码模式](../架构设计/模块分层与粘合代码模式.md)
- [非玩家单位行为整合方案](./../模块设计/非玩家单位行为整合方案.md)
