# 剪枝报告：程序弃用内容清查与处置建议

> 作者：代码分析 | 创建日期：2026-08-11
>
> 本文档记录对游戏工程（`d:\Unity\Spaceship`）现有程序实现的弃用内容清查结果，为代码剪枝提供依据。判定依据为：**代码标记、引用关系（场景 / 预制体 / 代码 / 菜单）、文档标注、时间线一致性**。

> ⚠️ **范围约定（2026-08-11）**：**SequenceMap 的全部内容（含 `Assets/Framework/SequenceMap/` 与 `Assets/wuqiwusheng/SequenceMap/`）一律不改动**，不在本次剪枝范围内。下文即使出现相关文件名，也仅作背景说明，不代表删除建议。

---

## 一、清查范围与方法

| 项 | 说明 |
|----|------|
| 主工程 | `d:\Unity\Spaceship`（150 个 C# 脚本，**不含 SequenceMap**） |
| 对照工程 | `d:\Unity\Spaceship_0`（113 个 C# 脚本，修改停留在 2026-08-05，**已确认废弃的旧工程副本**，2026-08-15 确认） |
| 文档库 | `d:\OneDrive\MyProjects\SpaceShip`（设计文档 / 重构清单） |
| 方法 | ① 全工程检索 `[Obsolete]`、`FormerlySerializedAs`、`HideInInspector` 等标记；② 按 GUID 核对每个组件在场景 / 预制体中的挂载；③ 统计每个类的引用文件；④ 对照「重构执行清单-死代码清理与归档」「工具清单」等既有文档 |

---

## 二、明确弃用的内容（建议删除）

### 2.1 带 `[Obsolete]` 标记的类

| 文件 | 标记信息 | 引用情况 |
|------|----------|----------|
| `Assets/Scripts/Gameplay/Linking/SnapSeatExecutor.cs` | `[Obsolete("改用 SpringSeatExecutor：弹簧力 + 临时摩擦吸附，不再硬锁位置。")]` | 无任何场景 / 预制体 / 代码引用（仅自引用） |

> 保留硬锁贴合实现仅作对照，运行时默认使用 `SpringSeatExecutor`，属于已完全弃用代码。

### 2.2 完全无引用的死代码文件

| 文件 | 内容 | 依据 |
|------|------|------|
| `Assets/sequence.cs` | Assets 根目录的 `Hello World` 调试脚本 | 无任何引用，纯残留 |
| `Assets/Scripts/Tools/SpaceshipPrototypeBootstrap.cs` | 旧场景兼容入口，转发到 `AssemblyPlaytestBootstrap` | 无场景挂载；[工具清单](./../工具与编辑器/工具清单.md) 标注「旧场景兼容包装」 |
| `Assets/Scripts/Tools/AssemblyPlaytestBootstrap.cs` | 旧版全运行时装配入口（自动建导演/飞船/残骸/地图） | 无场景挂载；工具清单标注「旧版全运行时装配入口（正式手工场景不再依赖）」；`ManualSceneWiringValidator` 对残留此组件的场景输出警告 |
| `Assets/Scripts/Tools/Editor/AssemblyPlaytestSceneBuilder.cs` | 旧版场景构建菜单（`Spaceship/飞船/旧版/创建自动飞船组装测试场景`） | 依赖不存在的 `SpaceshipGameSystem.prefab` 与不存在的 `SpaceshipAssemblyPlaytest.unity`；与 `AssemblyPlaytestBootstrap` 同为旧链路 |
| `Assets/Scripts/Tools/Editor/EditorFontRecovery.cs` | 一次性编辑器字体污染修复工具（菜单含「一次性」） | 无引用 |
| `Assets/Scripts/Tools/Editor/ShipBlueprintCargoMigrationTool.cs` | 一次性迁移工具（旧版类别库存 → 货物定义） | 迁移已完成，菜单 `Spaceship/工具/迁移蓝图初始库存到货物定义` 为一次性用途 |
| `Assets/Editor/ShopSetupTool.cs` | 一次性商店装配工具 | 代码注释明确「执行完成后可删除本文件」 |
| `Assets/Scripts/Tools/SoundTestTools/AudioTestEventEmitter.cs` | 音频测试事件发射器 | 无场景挂载 |
| `Assets/Scripts/Tools/SoundTestTools/FXTestEventEmitter.cs` | 特效测试事件发射器 | 无场景挂载 |

### 2.3 遗留测试资产（按开发者归属目录，除 SequenceMap 外）

| 目录 / 文件 | 内容 | 依据 |
|-------------|------|------|
| `Assets/wuqiwusheng/SequenceMap/Database/happy.asset`、`test2.asset`、`新流程图.asset` | 序列图框架的示例/测试图资产 | ⚠️ **在剪枝范围外，保留不动**（SequenceMap 内容一律不改） |
| `Assets/wuqiwusheng/SequenceMap/Script/happy.cs` | 由 `happy.asset` 生成的运行时代码 | ⚠️ **在剪枝范围外，保留不动** |

> 说明：`Assets/wuqiwusheng/SequenceMap/` 整体属于 SequenceMap 的一部分，按范围约定保留。`Assets/Framework/SequenceMap/` 代码中出现的 `Wuqiwusheng.SequenceMap.Generated` 是代码生成命名空间的字符串常量（历史遗留命名），与删除决策无关。

### 2.4 无引用的场景文件

| 场景 | 状态 | 依据 |
|------|------|------|
| `Assets/Scenes/Test/PlayableSceneVerify.unity` | ✅ **保留不动** | 该场景仅是尚未更新到最新版本，并非弃用内容（2026-08-11 确认） |
| `Assets/Framework/SequenceMap/test/New Scene.unity` | 框架测试残留场景（含 `SequenceMapGraphRunner`） | ⚠️ **在剪枝范围外，保留不动**（SequenceMap 内容一律不改） |
| `Assets/Scenes/Test/局内场景模板.unity` | 与 `ShipTest.unity` 高度重复的模板场景 | 不在构建列表，疑似模板存档（**需人工确认是否仍作为模板保留**） |

---

## 三、旧版兼容层（暂保留，建议后续收敛）

以下内容**当前仍被运行时读取或 API 调用**，不属于「完全弃用」，但均为旧版数据 / 接口的兼容路径，剪枝时应与存档版本策略一并评估：

### 3.1 旧版序列化兼容字段（资产反序列化依赖）

| 位置 | 字段 | 说明 |
|------|------|------|
| `Scripts/DataRuntime/ModuleDefinition.cs` | `turnSpeedBonus` | `[HideInInspector]`，仅旧资产读取兼容，新动力模块不再配置 |
| `Scripts/Gameplay/Ships/Cargo/ShipCargoHold.cs` | `legacyCategory` 等 | `[FormerlySerializedAs("Type")]` 等旧字段迁移别名 |
| `Scripts/DataRuntime/ShipBlueprint.cs` | `cargoType` | 无货物定义时的旧版类别兼容值 |
| `Scripts/Core/ShipRuntimeSnapshot.cs` | `category` | 旧版类别兼容值 |

### 3.2 旧版兼容 API（供既有调用方 / 验收使用）

| 位置 | 成员 | 说明 |
|------|------|------|
| `Scripts/Gameplay/Linking/LinkSessionDriver.cs` | `ActiveSession` | 兼容单会话读取，实际为多会话 |
| `Scripts/Gameplay/Workshop/WorkshopWarehouse.cs` | `ModuleStacks` | 兼容旧遍历入口 |
| `Scripts/Gameplay/Ships/Assembly/ShipAssemblyInteractionController.cs` | `Initialize` 重载 | 兼容旧签名 |
| `Scripts/Gameplay/Ships/Movement/ShipMotionController.cs` | 旧版键盘读取回退 | 未注入输入源时的兼容兜底 |
| `Scripts/Gameplay/NonPlayerUnits/NonPlayerUnitController.cs` | `NonPlayerUnitShipState` 概略状态 | 兼容旧 UI 查询 |
| `Scripts/Validation/RadarAcceptanceScenario.cs` | 6 个旧版雷达测试场景引用 | `[HideInInspector]` 旧版场景兼容引用，新场景统一读取飞船场景名册 |

---

## 四、待确认项

| 项 | 状态 | 需要确认的问题 |
|----|------|----------------|
| ~~`d:\Unity\Spaceship_0`~~ | ✅ **已确认废弃**（2026-08-15） | 不再使用，可归档或删除 |
| `Scenes/Test/局内场景模板.unity` | 与 `ShipTest` 高度重复，不在构建列表 | 是否仍作为场景模板保留？ |
| `RadarAcceptancePanel` / `ShieldRadarScenario` / `RadarAcceptanceScenario` | 未挂载到任何场景 | 是否仍有对应验收场景计划？若无，属未使用验收组件 |

---

## 五、与既有重构清单的衔接

《重构执行清单-死代码清理与归档.md》（2026-08-02）列出的 4 个 `wuqiwusheng/NonPlayerUnitBehavior/*` 死代码文件**已不存在**（已清理），`Assets/Mailk/` 原型也已删除。

本次清查发现的新增项（**均不含 SequenceMap**）：

- `sequence.cs`（Assets 根目录调试脚本）
- `AssemblyPlaytestSceneBuilder` / `AssemblyPlaytestBootstrap` / `SpaceshipPrototypeBootstrap` 旧链路（工具清单已标注旧版但尚未删除）
- `ShopSetupTool`、`ShipBlueprintCargoMigrationTool`、`EditorFontRecovery` 一次性工具
- `SoundTestTools` 两个测试发射器

---

## 六、处置建议与执行顺序

> 执行进度：**第一批已于 2026-08-11 完成**。

1. **第一批（已完成 2026-08-11）**：`sequence.cs`、`SnapSeatExecutor.cs`（含 `.meta`）已删除。**不包含任何 SequenceMap 文件**。
2. **第二批（旧链路工具）**：`AssemblyPlaytestBootstrap`、`SpaceshipPrototypeBootstrap`、`AssemblyPlaytestSceneBuilder`、`SoundTestTools`。删除前用 `ManualSceneWiringValidator` 扫描场景中是否仍有组件挂载残留。
3. **第三批（一次性工具，经确认后删除）**：`ShopSetupTool`、`ShipBlueprintCargoMigrationTool`、`EditorFontRecovery`。
4. **兼容层**：随存档版本策略（`GameSaveService` 与 `schemaVersion` 升级）统一评估，不单独处理。
5. 删除后运行 `Spaceship/飞船/验证手工场景装配` 与全部验收脚本，确认无回归。
6. 同步更新 [整体架构](./整体架构.md) 的类型清单与 [工具清单](./../工具与编辑器/工具清单.md)。

---

## 相关文档

- [重构执行清单-死代码清理与归档](./重构执行清单-死代码清理与归档.md)
- [工具清单](./../工具与编辑器/工具清单.md)
- [整体架构](./整体架构.md)
- [模块分层与粘合代码模式](./模块分层与粘合代码模式.md)
