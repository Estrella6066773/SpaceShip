---
description: >-
  Terminal UTF-8 Encoding — Before executing commands with py (Python),
  PowerShell, or CMD, ensure Chinese output encoding is properly configured
  (e.g., set code page to 65001) to avoid garbled Chinese characters.
  | 终端中文适配 — 使用 py、PowerShell 或 cmd 执行命令前，先做好中文输出适配
  （如设置代码页 65001），避免中文乱码
alwaysApply: true
---

# 终端中文适配

在 Windows 下使用 `py`（Python）、PowerShell 或 CMD 执行命令时，中文输出可能乱码。执行前需先做好中文适配。

## 已知问题（2026-08-15 实测记录）

- **PowerShell / CMD 列表输出中文文件名乱码**：磁盘上的 UTF-8 中文路径被按本地代码页（GBK 936）解码，`ls`、`Get-ChildItem` 等输出的中文全部变成乱码。
- **`chcp 65001` 并非总是生效**：同一个 PowerShell 会话里，已建立输出管道时切换代码页不保证生效，列表输出仍可能是乱码。
- **`PYTHONUTF8=1` 不控制 stdout**：`PYTHONUTF8` 只影响文件读写与输入编码，Python 的 `print()` 输出中文仍按控制台代码页转码，照样乱码。
- **rg 搜中文参数可能搜不到**：命令行参数在非 UTF-8 终端里被转码损坏，导致关键词本身失真、搜索返回空。

## 首选方案：避免在终端处理中文

终端对中文的适配不可靠，**凡是涉及中文文件名、中文内容或中文关键词搜索，一律改用专用工具**，不经过终端代码页：

- 读写文件：Read / Write / StrReplace
- 搜索：Grep / Glob
- 上述工具对 UTF-8 中文路径与内容处理正常，不受控制台代码页影响。

终端只用于与中文无关的系统命令（git、编译、进程等）。

## 必须使用终端时的稳定方案

### PowerShell

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
$env:PYTHONUTF8 = "1"
$env:PYTHONIOENCODING = "utf-8"
```

- `[Console]::OutputEncoding` 决定命令输出向控制台写入的编码（比 `chcp 65001` 可靠）。
- `$OutputEncoding` 决定传给外部命令的参数编码，避免 rg 等工具的中文参数损坏。

### CMD

```cmd
chcp 65001
set PYTHONUTF8=1
set PYTHONIOENCODING=utf-8
```

### Python

统一使用 `py -X utf8` 启动，一次解决文件读写、stdin 与 stdout 全部编码：

```powershell
py -X utf8 script.py
```

或用环境变量等价物：`PYTHONUTF8=1` + `PYTHONIOENCODING=utf-8`（两者缺一不可，仅设 `PYTHONUTF8` 不够）。

### 输出落盘再读取（兜底）

若命令输出含中文且仍乱码，不要反复尝试终端转码，把输出重定向到文件后用 Read 读取：

```powershell
py -X utf8 script.py | Out-File -Encoding utf8 result.txt
```

## 验证

执行任何含中文的命令前，先输出一条中文测试串确认无乱码：

```powershell
Write-Output "中文测试"
```

确认正常后再执行目标命令；若仍乱码，改用专用工具处理，不要在终端上继续折腾。

## 注意事项

- 可在同一条命令中先设置编码再执行目标命令。
- 若命令输出包含中文且被重定向或解析，务必先确认输出编码为 UTF-8，否则可能出现编码错误。
- 不要依赖 `chcp 65001` 或 `PYTHONUTF8=1` 单独生效；以本节「稳定方案」为准。
