---
description: >-
  Event-Subscription Communication — Communication between different objects
  MUST default to the Event-Subscription framework. Direct method calls are only
  allowed in special cases (synchronous return values, ordering guarantees, etc.).
  | 对象间沟通优先使用事件-订阅 — 不同对象间沟通默认使用事件-订阅框架，仅在需要同步
  结果、顺序保证等特殊情况才允许直接调用
alwaysApply: true
---

# 对象间沟通优先使用事件-订阅

项目后续开发中，不同对象之间的沟通，除非情况特殊，否则**一律使用事件-订阅（Event-Subscription）框架**，不得直接互相持有引用、直接调用对方方法。

## 默认行为

涉及两个及以上对象之间的信息传递时，首选事件-订阅：

- 事件源只广播「已发生的事实」，不感知订阅者
- 订阅者通过 `event Action<T>` 注册处理器，收到事件后自行响应
- 事件解耦多个系统（玩法 → UI / Audio / FX）时尤其必须使用事件

## 例外情况（允许直接调用）

仅当满足以下任一条件时，才可直接调用对方方法，并优先使用对应形式：

| 例外条件 | 采用形式 |
|----------|----------|
| 需要同步获得返回值 | Query：只读属性或返回值方法 |
| 必须立即执行、有顺序或失败结果要求 | Command：公共方法 |
| 初始化时注入窄接口 | Callback：`Func` / `Action` 注入 |
| 对象自身的局部内部逻辑 | 私有方法（无需事件） |
| 场景装配期依赖注入 | `[SerializeField]` 显式引用（仅用于装配） |

## 实现约定

- 事件源为场景实例（`SpaceshipEventSource` 体系），不得引用任何订阅者
- 事件载荷使用 `readonly struct`，避免堆分配
- 事件源不保存玩法状态、不执行业务规则
- 新增事件时：定义 `readonly struct` 载荷 → 在事件源声明 `event Action<T>` → 广播方 Raise → 订阅方注册

## 禁止的反模式

- 玩法系统直接调用 Audio / FX 的具体 API
- UI 反向修改玩法系统状态
- 事件源反向引用订阅者
- 为「返回结果 / 顺序保证 / 失败处理」强行套用事件

## 参考

- 事件系统设计：`03-程序设计/按领域分类/模块设计/事件系统.md`
- 编码规范「通信模式分离」章节：`03-程序设计/按领域分类/规范与标准/编码规范概要.md`
- 工程代码：`d:\Unity\Spaceship\Assets\Scripts\Events\`
