---
description: 当前开发阶段不考虑新旧存档的兼容与迁移；变更存档结构时直接更新 schemaVersion，不编写旧档迁移/适配逻辑
alwaysApply: true
---

# 存档版本维护

当前开发阶段**不考虑新旧存档的兼容与迁移问题**（2026-08-10 确认）。

## 默认行为

- 存档结构发生变更时，直接更新 `SaveData.schemaVersion` 的默认值并继续开发
- **不编写**旧版本存档的读取适配、字段补齐、数据迁移或跨版本校验逻辑
- 不维护针对旧存档的兼容代码路径
- 旧存档读取异常时按现有 `GameSaveService` 兜底逻辑处理（读取失败回退默认存档）

## 后续版本

待版本稳定、进入正式发布阶段后，再另行设计并补充存档迁移策略。届时在本规则中增补迁移约定。

## 参考

- 存档实现：`d:\Unity\Spaceship\Assets\Scripts\Core\GameSaveService.cs`
- 决策背景：任务系统接入存档时，不再为旧档（schemaVersion=4）设计迁移方案，直接采用新版本号
